changelog:
  v2.1.0:
    - type: summary
      description: |
        This release adds support for a new authentication method, mTLS certificates.
        We've gotten lots of requests for this to happen.
    - type: feature
      description: "Added new authentication method (mTLS)"
      scope: "authentication"
      issue: 123
    - type: bugfix
      description: "Fixed memory leak in service worker"
      scope: "worker"
      pr: 456
  v2.0.0:
    - type: summary
      description: "Major release with breaking changes"
    - type: breaking
      description: "Updated API to use async patterns"
      scope: "api"
    - type: feature
      description: "Added support for .NET 10"
      scope: "runtime"